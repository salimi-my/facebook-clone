import { atom } from 'recoil';

export const inputModalState = atom({
  key: 'inputModalState',
  default: false
});

export const photoModalState = atom({
  key: 'photoModalState',
  default: false
});
